apiVersion: v1
kind: Service
metadata:
  name: {{ include "mario-app.fullname" . }}
  labels:
    app: {{ include "mario-app.name" . }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      nodePort: {{ .Values.service.nodePort }}
  selector:
    app: {{ include "mario-app.name" . }}
